# Backend Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/kilbil
PORT=5000

# CORS Configuration - Required for separate frontend/backend deployments
# Comma-separated list of allowed frontend URLs
# Development: localhost variations are automatically allowed
# Production: Set to your frontend deployment URL
# Example: https://your-frontend.vercel.app or https://your-frontend.netlify.app
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Frontend Configuration (for Vite)
# Set this to your backend URL when deploying frontend separately
# Example: https://your-backend.replit.dev
# Leave empty to use the same origin (default behavior)
VITE_API_URL=http://localhost:5000

# Admin Session Secret
SESSION_SECRET=your-secure-random-secret-here-change-in-production
